name: PR Coverage Report

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Debug - Check coverage files
        if: always()
        run: |
          echo "=== Working directory ==="
          pwd
          echo "=== All files ==="
          find . -name "coverage-summary.json" -type f
          echo "=== Coverage directory ==="
          ls -la coverage/ || echo "No coverage directory"
          echo "=== Coverage files ==="
          ls -la coverage/*.json || echo "No JSON files"

      - name: Download base coverage
        uses: dawidd6/action-download-artifact@v3
        with:
          workflow: coverage-base.yml
          branch: main
          name: base-coverage
          path: base-coverage
        continue-on-error: true

      - name: Create coverage report
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            // Check if current coverage exists
            let currentCoverage;
            try {
              currentCoverage = JSON.parse(
                fs.readFileSync('coverage/coverage-summary.json', 'utf8')
              );
            } catch (error) {
              console.log('❌ No coverage file generated. Tests may have failed.');
              
              // Post error comment
              const errorComment = `## Coverage Report
              
              ⚠️ **Unable to generate coverage report**
              
              Coverage file not found. This usually means:
              - Tests failed to run
              - \`npm run test:coverage\` didn't generate coverage
              - Coverage directory is not created
              
              Please check the test execution logs above.`;
              
              const { data: comments } = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
              });
              
              const botComment = comments.find(comment => 
                comment.user.type === 'Bot' && 
                comment.body.includes('## Coverage Report')
              );
              
              if (botComment) {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: botComment.id,
                  body: errorComment
                });
              } else {
                await github.rest.issues.createComment({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  body: errorComment
                });
              }
              
              return; // Exit early
            }

            // Check if base coverage exists
            let baseCoverage;
            let isFirstRun = false;
            try {
              baseCoverage = JSON.parse(
                fs.readFileSync('base-coverage/coverage-summary.json', 'utf8')
              );
            } catch (error) {
              console.log('⚠️ No base coverage found. This is the first run.');
              isFirstRun = true;
              // Use empty baseline
              baseCoverage = { 
                total: { 
                  lines: { pct: 0, total: 0, covered: 0 },
                  branches: { total: 0 },
                  statements: { total: 0 },
                  functions: { total: 0 }
                } 
              };
            }

            const current = currentCoverage.total;
            const base = baseCoverage.total;

            // Calculate stats
            const coverageDiff = current.lines.pct - base.lines.pct;
            const linesDiff = current.lines.total - base.lines.total;
            const branchesDiff = current.branches.total - base.branches.total;
            const hitsDiff = current.lines.covered - base.lines.covered;
            const missesDiff = (current.lines.total - current.lines.covered) - (base.lines.total - base.lines.covered);

            // Header
            let header;
            if (isFirstRun) {
              header = `## 📊 First Coverage Report\n\nCurrent coverage: **${current.lines.pct.toFixed(2)}%**\n\n_This is the first coverage report. Future PRs will show diff against this baseline._`;
            } else {
              const direction = coverageDiff > 0 ? 'increase' : coverageDiff < 0 ? 'decrease' : 'remain the same';
              const emoji = coverageDiff > 0 ? '📈' : coverageDiff < 0 ? '📉' : '➡️';
              header = `${emoji} Merging **#${context.issue.number}** into **main** will ${direction} coverage by \`${Math.abs(coverageDiff)}%\`.`;
            }

            // Coverage Diff Table
            const pad = (num, width = 8) => String(num).padStart(width);
            const padFloat = (num, width = 7) => num.toFixed(2).padStart(width);

            const diffTable = `
            \`\`\`diff
            @@           Coverage Diff            @@
            ##           main      #${context.issue.number}       +/-   ##
            ==========================================
            ${coverageDiff >= 0 ? '+' : '-'} Coverage   ${padFloat(base.lines.pct)}%   ${padFloat(current.lines.pct)}%   ${coverageDiff >= 0 ? '+' : ''}${padFloat(coverageDiff)}%
            ==========================================
              Files      ${pad(base.statements.total)}    ${pad(current.statements.total)}    ${linesDiff >= 0 ? '+' : ''}${pad(linesDiff)}
              Lines      ${pad(base.lines.total)}    ${pad(current.lines.total)}    ${linesDiff >= 0 ? '+' : ''}${pad(linesDiff)}
              Branches   ${pad(base.branches.total)}    ${pad(current.branches.total)}    ${branchesDiff >= 0 ? '+' : ''}${pad(branchesDiff)}
            ==========================================
            ${hitsDiff >= 0 ? '+' : '-'} Hits       ${pad(base.lines.covered)}    ${pad(current.lines.covered)}    ${hitsDiff >= 0 ? '+' : ''}${pad(hitsDiff)}
            ${missesDiff >= 0 ? '+' : '-'} Misses     ${pad(base.lines.total - base.lines.covered)}    ${pad(current.lines.total - current.lines.covered)}    ${missesDiff >= 0 ? '+' : ''}${pad(missesDiff)}
            \`\`\`
            `;

            // Impacted Files (skip for first run)
            let impactedTable = '';
            if (!isFirstRun) {
              const impactedFiles = [];
              for (const file of Object.keys(currentCoverage)) {
                if (file === 'total') continue;
                if (file.includes('.test.') || file.includes('.spec.')) continue;
                
                const currentPct = currentCoverage[file]?.lines.pct || 0;
                const basePct = baseCoverage[file]?.lines.pct || 0;
                const diff = currentPct - basePct;
                
                if (Math.abs(diff) > 0.01) {
                  impactedFiles.push({
                    name: file,
                    current: currentPct.toFixed(2),
                    diff: diff.toFixed(2),
                    arrow: diff < -0.01 ? '⬇️' : diff > 0.01 ? '⬆️' : '➡️'
                  });
                }
              }
              
              if (impactedFiles.length > 0) {
                impactedTable = '\n## Impacted Files\n\n| File | Coverage Δ |\n|------|------------|\n';
                impactedFiles.forEach(f => {
                  impactedTable += `| \`${f.name}\` | \`${f.current}% (${f.diff >= 0 ? '+' : ''}${f.diff}%)\` ${f.arrow} |\n`;
                });
              }
            }

            // Final comment
            const comment = `## Coverage Report\n\n${header}\n${diffTable}${impactedTable}`;

            // Find and update or create comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment => 
              comment.user.type === 'Bot' && 
              comment.body.includes('## Coverage Report')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: comment
              });
            } else {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            }
